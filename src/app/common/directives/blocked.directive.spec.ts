import { BlockedDirective } from './blocked.directive';

describe('BlockedDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockedDirective();
    expect(directive).toBeTruthy();
  });
});
